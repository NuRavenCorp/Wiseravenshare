import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.nuravencorp.wiseravenshare',
  appName: 'Wiseravenshare',
  webDir: 'dist'
};

export default config;
