VITE_API_URL=/api

# Set to true only when backend auth is unavailable during local development.
VITE_AUTH_BYPASS=false

# Optional: auto-login a local dev user when bypass is enabled.
VITE_AUTH_AUTO_LOGIN=false
