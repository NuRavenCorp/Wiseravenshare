apiVersion: v1
kind: Service
metadata:
  name: api
spec:
  selector:
    app.kubernetes.io/name: nu-raven
    app.kubernetes.io/component: api
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
